global filesystem = {
  "home" = {};
  "bin" = {
    "bash" = "println(\"You want another shell? :(\");";
  };
};

fn print(text: String) {
  screen_buffer = screen_buffer + text;
}

fn println(text: String) {
  screen_buffer = screen_buffer + text + "\n";
}

let bin = filesystem["bin"];
eval(bin["bash"]);
